/**
 * Core types for LaunchKit applications.
 *
 * Extracted from endorsed and launch-lab, generalized for all LaunchKit consumers.
 *
 * NOTE: NextAuth type augmentations are in ./auth.ts - import separately if needed.
 */

// Re-export all core types
export * from "./core.js";
